@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirige al usuario a la ruta /login y fuerza la recarga (true)
        Navigation.NavigateTo("/login", true);
    }
}

